import * as React from 'react';
import { Text, View, StyleSheet, Image } from 'react-native';

export default function BlocRouge() {
  return (
    <View style={styles.container}>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#EF4135",
    width: '20vw',
    height: '20vw',
    marginBottom: '5vh',
    borderRadius: '5vw',
    padding: 0,
  },
});
